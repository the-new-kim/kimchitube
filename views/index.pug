extends base
include mixins/video

block body
    if(!!videos.length)
        div.video-grid
            each video in videos
                +video(video)
    else
        span.empty__message No video found 
